<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.khwy.mapper.RoleMapper">
	<select id="findById" parameterType="integer" resultType="com.khwy.pojo.Role">
		select 
			roleid,rolename,perids 
		from
			t_role
		<where>
			1=1
			and roleid = #{roleid}
		</where>
	</select>
	<select id="list" resultType="com.khwy.pojo.Role">
		select r.*,o.*,u.* from t_role r,t_org o ,t_user u where r.orgid=o.orgid and r.operid=u.userid
	</select>
</mapper>